## 1.0.0-beta.2

- Add initial airline data
- Add initial car rental data
- Add initial hotel data
- Add `rewardLink` field to `LoyaltyBrand` model
- Refactor `LoyaltyBrand` model to include `operatorName` field


## 1.0.0-beta.1

- Initial beta version.
- Structured models for `LoyaltyBrand`, `LoyaltyTier`, `QualificationPath`, and `MilestoneRequirement`.
- Pre-populated hotel database (Marriott, Hyatt, Hilton, IHG, Choice, Wyndham).
- Pre-populated car rental database (National, Hertz, Avis, Enterprise).
- Baseline structure for future airline integrations.
